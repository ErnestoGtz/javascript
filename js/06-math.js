let resultado;

resultado = Math.PI;
resultado = Math.round(2.6);
resultado = Math.ceil(2.6); // Ceil redondea hacia arriba
resultado = Math.floor(2.6); // Floor redondea hacia abajo
resultado = Math.sqrt(144);
resultado = Math.abs(-200);
resultado = Math.min(3, 5, 10);
resultado = Math.max(3, 5, 10);

console.log(resultado);
